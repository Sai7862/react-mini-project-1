🌦 React Mini Weather Application

📌 Project Description

This is a React-based mini project that demonstrates API integration, state-based re-rendering, and responsive UI design using Material-UI components.
The application fetches real-time weather data from the OpenWeather API and dynamically renders the results based on user input.

⸻

🚀 Technologies Used
	•	React.js
	•	Material-UI (MUI)
	•	OpenWeather API
	•	Vite
	•	JavaScript (ES6)

⸻

✨ Features
	•	Search weather by city name
	•	Fetches real-time temperature, humidity, pressure, and weather conditions
	•	Dynamic UI updates using React useState
	•	Error handling for invalid city names
	•	Clean and responsive Material-UI design

⸻

⚙ How It Works

The user enters a city name in the input field.
The application sends a request to the OpenWeather API and receives real-time weather data.
Using React useState, the component re-renders automatically whenever new data is received, updating the UI without refreshing the page.

⸻

🎯 Learning Outcomes
	•	API integration using Fetch
	•	React state management using useState
	•	Component-based UI development
	•	Material-UI styling and layout
